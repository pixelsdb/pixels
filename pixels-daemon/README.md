# Presto-Daemon Configuration

## How to run netty client & server locally(or in IDEA)?
- pixels-server 
```
run TestServer
```

- pixels-client
```
run TestMetadataService
```

`Note` make sure the (host, port) in `TestMetadataService` is localhost
